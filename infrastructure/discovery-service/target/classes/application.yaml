spring:
  application:
    name: discovery-service

security:
  user:
    name: admin
    password: admin123

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false