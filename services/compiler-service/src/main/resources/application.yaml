spring:
  application:
    name: compiler-service
server:
  port: 8082
eureka:
  client:
    service-url:
      defaultZone: http://admin:admin123@localhost:8761/eureka/
  instance:
    prefer-ip-address: true
